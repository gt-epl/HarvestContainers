apiVersion: v1
kind: Service
metadata:
  name: dedup-secondary-svc
  labels:
    name: dedup-secondary-svc
spec:
  type: NodePort
  ports:
    - port: 20000
      nodePort: 30001
      name: http
  selector:
    name: dedup-secondary-server
