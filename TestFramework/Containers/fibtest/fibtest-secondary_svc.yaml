apiVersion: v1
kind: Service
metadata:
  name: fibtest-secondary-svc
  labels:
    name: fibtest-secondary-svc
spec:
  type: NodePort
  ports:
    - port: 20000
      nodePort: 30000
      name: http
  selector:
    name: fibtest-secondary-server
