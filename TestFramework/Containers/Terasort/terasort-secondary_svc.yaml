apiVersion: v1
kind: Service
metadata:
  name: terasort-secondary-svc
  labels:
    name: terasort-secondary-svc
spec:
  type: NodePort
  ports:
    - port: 20000
      nodePort: 30000
      name: http
  selector:
    name: terasort-secondary-server
