apiVersion: v1
kind: Service
metadata:
  name: cpubully-secondary-svc
  labels:
    name: cpubully-secondary-svc
spec:
  type: NodePort
  ports:
    - port: 20000
      nodePort: 30000
      name: http
  selector:
    name: cpubully-secondary-server
